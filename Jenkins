pipeline{
    agent any;
    stages{
        stage("pull"){
            steps{git url: "https://github.com/Swayamnakshane/wings.git",branch:"SwayamPipe"}
        }
        stage("build"){
            steps{sh "docker compose up -d"}
        }
    }
}
